#include "main.h"
/**
 * print_rev - prints out a string followed by a new line
 * @s: string printed out
 */
void rev_string(char *s)
{
	int i = 0;
	char *ns;

	while (s[i] != '\0')
	{
		ns[i] = s[i];
	}
		
}
